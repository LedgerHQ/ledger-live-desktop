name: CI
on:
  pull_request:
    branches:
      - "*"
  push:
    branches:
      - develop
      - master

jobs:
  type-checking:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@master
        with:
          node-version: 12.x
      - name: get yarn cache
        id: yarn-cache
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - uses: actions/cache@v2
        with:
          path: ${{ steps.yarn-cache.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: run headless tests
        uses: ledgerhq/actions/run-headless@v1.5.0
      - name: upload diffs to imgur
        uses: ledgerhq/actions/upload-images@v1.5.0
        id: imgur
        if: ${{ failure() }}
        with:
          path: tests/specs/__image_snapshots__/__diff_output__
          clientId: "11eb8a62f4c7927"
      - name: prepare comment body
        if: ${{ steps.comment.outputs.body != '' }}
        id: comment
        uses: ledgerhq/actions/prepare-comment-body@v1.5.0
        with:
          images: ${{ steps.imgur.outputs.images }}
      - name: comment
        if: ${{ github.event_name == 'pull_request' && success() }}
        uses: unsplash/comment-on-pr@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          msg: ${{ steps.comment.outputs.body }}
