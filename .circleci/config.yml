version: 2

defaults: &defaults
  working_directory: ~/ledger-live-desktop
  docker:
    - image: dockcross/windows-x64-posix

jobs:
# windows build
  build-windows:
    <<: *defaults

    steps:
      - checkout
      - run: yarn install
      - run: yarn release
