version: 2

jobs:
  build:
    macos:
      xcode: "9.0"
    environment:
        BASH_ENV: "~/.bashrc"
    steps:
#      - run: sudo apt-get install -y libudev-dev
      - checkout
      - run: |
          export N_PREFIX=$HOME
          npm install n
          export PATH=$PATH:node_modules/n/bin
          n latest
          n use latest --version
          yarn install
#      - run: curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash
#      - run: nvm use node
#      - run: node --version
#      - run: yarn lint
#      - run: ./node_modules/.bin/prettier -l "{src,webpack,.storybook,static/i18n}/**/*.js"
#      - run: yarn flow --quiet
      - run: yarn test
      - run: yarn release
