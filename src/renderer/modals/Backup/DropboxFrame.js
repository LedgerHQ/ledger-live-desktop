// @flow
/*import React, { useMemo } from "react";
import styled from "styled-components";
import { useSelector } from "react-redux";
import { Trans } from "react-i18next";
import ToolTip from "~/renderer/components/Tooltip";
import Box from "~/renderer/components/Box/Box";
import Text from "~/renderer/components/Text";
// CAN I USE THIS WITH THE OLD COMPONENT ? import setDrawer from "~/renderer/drawer/Provider";

const DropboxFrame = ({ isOpened, onClose }: { isOpened: boolean, onClose: () => void }) => {
  return (
    <SideDrawer isOpen={isOpened} onRequestClose={onClose} direction="left">
    <Text ff="Inter|SemiBold" fontSize={22} mb={20} color={"palette.text.shade100"}>
      PLEASE PLEASE WORK
    </Text>
  );
};

export default DropboxFrame;*/
